import { atom } from 'jotai'
import { Socket } from 'socket.io-client'

// export const webSocketAtom = atom<Socket | null>(null)

// export const sessionAtom = atom<Session | null>(null)

// export const botsAtom = atom<{
//     id: string
//     name: string
//     socketId: string
// }[] | null>(null)